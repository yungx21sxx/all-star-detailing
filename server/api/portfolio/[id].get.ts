import portfolioService from "~/server/service/portfolio.service";

export default defineEventHandler(async (event) => {
    // const { id } = event.context.params
    // return await portfolioService.getCar(parseInt(id))
})